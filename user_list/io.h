#ifndef IO_H
#define IO_H
#include <stdio.h>
#include <stdlib.h>
#include "colors.h"

// Print a list of integers
void print_list(const int* list, int size);

// Get the user's choice
int get_user_choice();

// Display the menu
void display_menu();

#endif

